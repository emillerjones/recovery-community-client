import { useState } from "react";
import { PUBLIC_STORIES, SHAWN_MEMORIAL } from "../data/publicStories";
import { StoriesConceptBody } from "./StoriesConceptBody";
import "./Stories7.css";
export default function Stories7(){const [light,setLight]=useState({x:62,y:46});function move(e){const r=e.currentTarget.getBoundingClientRect();setLight({x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100})}return <main className="s7"><section className="s7-reveal" onPointerMove={move} style={{"--lx":`${light.x}%`,"--ly":`${light.y}%`}}><div className="s7-copy"><p>Move through the collection</p><h1>Take the time to witness them.</h1><span>Move the light across each portrait. Tap any person to read their words.</span></div><div className="s7-faces">{PUBLIC_STORIES.map((s,i)=><a className={i===0?"featured":""} href={`#full-${s.slug}`} key={s.slug}><img src={s.photo} alt={s.name}/><span>{s.name}</span></a>)}<a className="memorial" href="#full-shawn"><img src={SHAWN_MEMORIAL.photo} alt="Shawn"/><span>Shawn · In memoriam</span></a></div><div className="s7-lamp"/><a href="#collection" className="s7-cue">Continue to their words ↓</a></section><StoriesConceptBody/></main>}
